import { CopiarTextoDirective } from './copiar-texto.directive';

describe('CopiarTextoDirective', () => {
  it('should create an instance', () => {
    const directive = new CopiarTextoDirective();
    expect(directive).toBeTruthy();
  });
});
